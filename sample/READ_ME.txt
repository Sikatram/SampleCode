This program will attempt to detect metaphors through a piece of text.

The text file it will read from is named "context.txt"

This file contains a sample of text that was scraped from a news media site.

If you would like test you own text, simply replace the text with the news text file.

To run the program simply enter:
$python3 metaphor_analysis.py